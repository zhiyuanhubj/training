diff --git a/swift/arguments/base_args/base_args.py b/swift/arguments/base_args/base_args.py
index 326fb6d..635927e 100644
--- a/swift/arguments/base_args/base_args.py
+++ b/swift/arguments/base_args/base_args.py
@@ -74,6 +74,8 @@ class BaseArguments(GenerationArguments, QuantizeArguments, DataArguments, Templ
         packing (bool): Whether to enable packing of datasets. Default is False.
         packing_length (Optional[int]): Length of packing. Default is None.
         packing_num_proc (int): Number of processes used for packing, Default is 1.
+        packing_interval (int): Number of streaming source rows encoded before each
+            packing decision. Default is 128.
         packing_strategy (Literal['binpack', 'sequential']): Packing algorithm. 'binpack' (default) uses
             best-fit-decreasing bin packing (reorders samples); 'sequential' uses order-preserving greedy
             packing (next-fit: a single open pack, flushed when the next sample doesn't fit) so the sample
@@ -105,6 +107,7 @@ class BaseArguments(GenerationArguments, QuantizeArguments, DataArguments, Templ
     packing: bool = False
     packing_length: Optional[int] = None
     packing_num_proc: int = 1
+    packing_interval: int = 128
     packing_strategy: Literal['binpack', 'sequential'] = 'binpack'
     lazy_tokenize: Optional[bool] = None
     # hub
diff --git a/swift/arguments/base_args/data_args.py b/swift/arguments/base_args/data_args.py
index 01b8b0f..95c8a7c 100644
--- a/swift/arguments/base_args/data_args.py
+++ b/swift/arguments/base_args/data_args.py
@@ -84,8 +84,16 @@ class DataArguments:
     dataset_shuffle: bool = True
     val_dataset_shuffle: bool = False
     streaming: bool = False
+    # Shard the final shuffled streaming source by Megatron DP rank and let
+    # every rank read locally instead of scattering large packed Python objects
+    # across nodes.
+    streaming_shard_by_dp: bool = False
     interleave_prob: Optional[List[float]] = None
-    stopping_strategy: Literal['first_exhausted', 'all_exhausted'] = 'first_exhausted'
+    stopping_strategy: Literal[
+        'first_exhausted',
+        'all_exhausted',
+        'all_exhausted_without_replacement',
+    ] = 'first_exhausted'
     shuffle_buffer_size: int = 1000
 
     download_mode: Literal['force_redownload', 'reuse_dataset_if_exists'] = 'reuse_dataset_if_exists'
diff --git a/swift/dataloader/dispatcher.py b/swift/dataloader/dispatcher.py
index 8111467..c872508 100644
--- a/swift/dataloader/dispatcher.py
+++ b/swift/dataloader/dispatcher.py
@@ -38,7 +38,11 @@ class DataLoaderDispatcher:
                 [next(base_iter) for _ in range(self.world_size)]
 
     def __iter__(self):
-        base_iter = iter(self.base_dataloader)
+        # Only rank zero of each DP group fetches and collates source data; all
+        # other ranks receive their batch through scatter_object_list. Creating
+        # iterators everywhere starts redundant streaming/packing workers and
+        # makes every rank independently decode the same multimodal prefix.
+        base_iter = iter(self.base_dataloader) if self.rank == 0 else None
         self._skip_batches(base_iter)
         while True:
             if self.rank == 0:
diff --git a/swift/dataset/loader.py b/swift/dataset/loader.py
index 11f00d4..a5b09c2 100644
--- a/swift/dataset/loader.py
+++ b/swift/dataset/loader.py
@@ -231,7 +231,11 @@ def load_dataset(
     shuffle: bool = False,
     streaming: bool = False,
     interleave_prob: Optional[List[float]] = None,
-    stopping_strategy: Literal['first_exhausted', 'all_exhausted'] = 'first_exhausted',
+    stopping_strategy: Literal[
+        'first_exhausted',
+        'all_exhausted',
+        'all_exhausted_without_replacement',
+    ] = 'first_exhausted',
     shuffle_buffer_size: int = 1000,
     use_hf: Optional[bool] = None,
     hub_token: Optional[str] = None,
@@ -268,7 +272,8 @@ def load_dataset(
             same length as datasets list. If None, datasets are concatenated instead. Default: None.
         stopping_strategy: Strategy when interleaving datasets of different lengths:
             - 'first_exhausted': Stop when shortest dataset is exhausted
-            - 'all_exhausted': Continue until all datasets are exhausted
+            - 'all_exhausted': Continue until all datasets are exhausted, oversampling as needed
+            - 'all_exhausted_without_replacement': Consume each selected source row exactly once
             Default: 'first_exhausted'.
         shuffle_buffer_size: Buffer size for shuffling in streaming mode. Larger values
             provide better randomization but use more memory. Default: 1000.
diff --git a/swift/megatron/arguments/megatron_args.py b/swift/megatron/arguments/megatron_args.py
index 5afaacd..533129d 100644
--- a/swift/megatron/arguments/megatron_args.py
+++ b/swift/megatron/arguments/megatron_args.py
@@ -487,6 +487,9 @@ class MegatronArguments(RLHFMegatronArgumentsMixin, MegatronTunerMixin):
     recompute_modules: List[str] = field(default_factory=lambda: ['core_attn'])
     train_iters: Optional[int] = None
     num_train_epochs: Optional[int] = None
+    # Treat train_iters as a safety ceiling and finish after one finite streaming
+    # source exposure instead of cycling it.
+    stop_at_dataset_end: bool = False
 
     masked_softmax_fusion: bool = True
     bias_dropout_fusion: bool = True
diff --git a/swift/megatron/init.py b/swift/megatron/init.py
index 6e19315..ba104c6 100644
--- a/swift/megatron/init.py
+++ b/swift/megatron/init.py
@@ -115,6 +115,7 @@ def _patch_unified_memory():
 
 
 def _patch_mcore_bridge():
+    import inspect
     import mcore_bridge
     from mcore_bridge import GPTBridge
     logger.info(f'mcore_bridge.__version__: {mcore_bridge.__version__}')
@@ -130,13 +131,16 @@ def _patch_mcore_bridge():
         processor=None,
         save_missing_weights: Union[bool, str] = False,
     ) -> None:
-        origin_save_weights(
-            self,
-            mg_models,
-            output_dir,
-            peft_format=peft_format,
-            max_shard_size=max_shard_size,
-            save_missing_weights=save_missing_weights)
+        save_kwargs = {
+            'peft_format': peft_format,
+            'max_shard_size': max_shard_size,
+        }
+        # mcore-bridge added save_missing_weights after the version currently
+        # pinned by this training environment. Passing the newer keyword to an
+        # older bridge crashes after the MCore checkpoint was already written.
+        if 'save_missing_weights' in inspect.signature(origin_save_weights).parameters:
+            save_kwargs['save_missing_weights'] = save_missing_weights
+        origin_save_weights(self, mg_models, output_dir, **save_kwargs)
         if processor is None or args is None:
             return
         hf_config = self.config.hf_config
diff --git a/swift/megatron/trainers/base.py b/swift/megatron/trainers/base.py
index d3925e1..9895f8d 100644
--- a/swift/megatron/trainers/base.py
+++ b/swift/megatron/trainers/base.py
@@ -1,9 +1,12 @@
 # Copyright (c) ModelScope Contributors. All rights reserved.
 import dataclasses
+import copy
 import logging
 import megatron.core
+import numpy as np
 import operator
 import os
+import random
 import shutil
 import torch
 import torch.nn
@@ -11,7 +14,7 @@ from abc import ABC, abstractmethod
 from contextlib import contextmanager, nullcontext
 from functools import partial
 from mcore_bridge import LoraParallelLinear
-from megatron.core import mpu
+from megatron.core import mpu, tensor_parallel
 from megatron.core.distributed import DistributedDataParallel as DDP
 from megatron.core.distributed import FullyShardedDataParallel as megatron_FSDP
 from megatron.core.distributed import finalize_model_grads
@@ -77,7 +80,14 @@ class BaseMegatronTrainer(ABC):
         self.data_collator = self._get_data_collator()
 
         self.state = TrainerState(max_steps=args.train_iters)
-        initialize_embedding = args.new_special_tokens or args.task_type == 'seq_cls'
+        # Fresh runs must initialize newly added token/classification weights.
+        # A resume restores those weights from MCore; reinitializing before load
+        # is both incorrect and incompatible with DP-sharded optimizer main_param.
+        initialize_embedding = (
+            (args.new_special_tokens or args.task_type == 'seq_cls')
+            and args.mcore_model is None
+            and args.mcore_adapter is None
+        )
         if initialize_embedding:
             for m in self.unwrapped_models:
                 self._initialize_embedding(m)
@@ -577,12 +587,88 @@ class BaseMegatronTrainer(ABC):
 
     def _prepare_data_iterator(self, train_dataset, val_dataset=None, use_origin_cyclic: bool = False):
         train_dataloader, val_dataloader = self._prepare_dataloader(train_dataset, val_dataset)
-        train_data_iterator = iter(self.cyclic_iter(train_dataloader, use_origin_cyclic=use_origin_cyclic))
+        if self.args.stop_at_dataset_end:
+            if not self.args.streaming:
+                raise RuntimeError('stop_at_dataset_end is only supported for streaming datasets')
+            train_data_iterator = iter(train_dataloader)
+        else:
+            train_data_iterator = iter(self.cyclic_iter(train_dataloader, use_origin_cyclic=use_origin_cyclic))
+        self._replay_streaming_data_iterator(train_data_iterator)
         val_data_iterator = None
         if val_dataset is not None:
             val_data_iterator = iter(self.cyclic_iter(val_dataloader, use_origin_cyclic=use_origin_cyclic))
         return train_data_iterator, val_data_iterator
 
+    @staticmethod
+    def _snapshot_training_rng():
+        tracker_states = tensor_parallel.get_cuda_rng_tracker().get_states()
+        return {
+            'python': random.getstate(),
+            'numpy': np.random.get_state(),
+            'torch': torch.get_rng_state(),
+            'cuda': torch.cuda.get_rng_state(),
+            'tracker': copy.deepcopy(tracker_states),
+        }
+
+    @staticmethod
+    def _restore_training_rng(state):
+        random.setstate(state['python'])
+        np.random.set_state(state['numpy'])
+        torch.set_rng_state(state['torch'])
+        torch.cuda.set_rng_state(state['cuda'])
+        tensor_parallel.get_cuda_rng_tracker().set_states(state['tracker'])
+
+    def _replay_streaming_data_iterator(self, train_data_iterator):
+        """Reconstruct the exact next dispatched batch after a streaming resume.
+
+        consumed_train_samples counts global *post-packing* dataset items. Replay
+        through the fully composed cyclic iterator so HF shuffle/interleave,
+        packing carry/bin boundaries, finite-stream cycles, DP grouping and
+        discarded partial tails all reach the same state as uninterrupted
+        training. Main/model RNG is restored afterwards; the dataloader has its
+        own deterministic generator.
+        """
+        args = self.args
+        consumed = self.state.consumed_train_samples
+        if not args.streaming or consumed <= 0:
+            return
+
+        dp = mpu.get_data_parallel_world_size()
+        per_round = dp * args.micro_batch_size
+        if consumed != self.state.iteration * args.global_batch_size:
+            raise RuntimeError(
+                f'inconsistent streaming cursor: consumed_train_samples={consumed}, '
+                f'iteration={self.state.iteration}, global_batch_size={args.global_batch_size}')
+        if consumed % per_round:
+            raise RuntimeError(
+                f'streaming cursor {consumed} is not divisible by DP*micro_batch_size={per_round}')
+        if args.packing and args.micro_batch_size != 1:
+            raise RuntimeError('exact packed streaming resume requires micro_batch_size=1')
+
+        rounds = consumed // per_round
+        expected_rounds = self.state.iteration * args.num_microbatches
+        if rounds != expected_rounds:
+            raise RuntimeError(
+                f'streaming replay mismatch: rounds={rounds}, '
+                f'iteration*num_microbatches={expected_rounds}')
+
+        if is_master():
+            logger.warning(
+                f'Replaying {rounds} dispatched streaming batch(es) to restore the exact '
+                f'post-packing data cursor ({consumed} global samples).')
+        rng_state = self._snapshot_training_rng()
+        report_every = max(rounds // 20, 1)
+        try:
+            for index in range(rounds):
+                next(train_data_iterator)
+                if is_master() and ((index + 1) % report_every == 0 or index + 1 == rounds):
+                    logger.info(f'Streaming resume replay: {index + 1}/{rounds} batches')
+        except StopIteration as exc:
+            raise RuntimeError(
+                f'cyclic streaming iterator ended during resume replay at {index + 1}/{rounds}') from exc
+        finally:
+            self._restore_training_rng(rng_state)
+
     def setup_model_training(self):
         args = self.args
         config = self.config
@@ -736,7 +822,21 @@ class BaseMegatronTrainer(ABC):
     def train(self, train_dataset, val_dataset):
         train_data_iterator, val_data_iterator = self.setup_training(train_dataset, val_dataset)
         while self.state.iteration < self.args.train_iters:
-            self.run_train_step(train_data_iterator, val_data_iterator)
+            try:
+                self.run_train_step(train_data_iterator, val_data_iterator)
+            except StopIteration:
+                if not self.args.stop_at_dataset_end:
+                    raise
+                # A partial gradient-accumulation group is intentionally
+                # discarded. The durable cursor remains at the last completed
+                # optimizer-step boundary.
+                self.optimizer.zero_grad()
+                if is_master():
+                    logger.info(
+                        f'Finite streaming dataset exhausted after {self.state.iteration} '
+                        f'complete optimizer step(s); saving the final durable state.')
+                self.save_checkpoint()
+                break
         self.finalize_training()
 
     def _determine_best_metric(self, metrics) -> bool:
diff --git a/swift/megatron/trainers/utils.py b/swift/megatron/trainers/utils.py
index 66d1e5d..b1c7568 100644
--- a/swift/megatron/trainers/utils.py
+++ b/swift/megatron/trainers/utils.py
@@ -335,6 +335,10 @@ class MegatronDataLoaderDispatcher(DataLoaderDispatcher):
 
 
 def build_streaming_dataloader(args, dataset, collate_fn):
+    # Keep worker seeding independent from model RNG. On resume the iterator is
+    # replayed from this seed to reconstruct worker-side preprocessing state.
+    data_generator = torch.Generator()
+    data_generator.manual_seed(args.data_seed)
     base_dataloader = torch.utils.data.DataLoader(
         dataset,
         num_workers=args.dataloader_num_workers,
@@ -343,7 +347,10 @@ def build_streaming_dataloader(args, dataset, collate_fn):
         batch_size=args.micro_batch_size,
         prefetch_factor=args.dataloader_prefetch_factor if args.dataloader_num_workers > 0 else None,
         persistent_workers=args.dataloader_persistent_workers if args.dataloader_num_workers > 0 else False,
+        generator=data_generator,
     )
+    if getattr(args, 'streaming_shard_by_dp', False):
+        return base_dataloader
     return MegatronDataLoaderDispatcher(base_dataloader)
 
 
diff --git a/swift/pipelines/train/sft.py b/swift/pipelines/train/sft.py
index 8d547f4..cfb3d9e 100644
--- a/swift/pipelines/train/sft.py
+++ b/swift/pipelines/train/sft.py
@@ -105,6 +105,9 @@ class SwiftSft(SwiftPipeline, TunerMixin):
             train_datasets, val_datasets = [], []
         if args.dataset or args.val_dataset:
             train_dataset, val_dataset = self._get_dataset()
+            if args.streaming and getattr(args, 'streaming_shard_by_dp', False):
+                train_dataset = self._shard_streaming_dataset_by_dp(train_dataset, 'train')
+                val_dataset = self._shard_streaming_dataset_by_dp(val_dataset, 'validation')
             train_dataset, val_dataset = self._encode_dataset(train_dataset, val_dataset, pre_process=pre_process)
             if train_dataset is not None:
                 train_datasets.append(train_dataset)
@@ -119,9 +122,32 @@ class SwiftSft(SwiftPipeline, TunerMixin):
         if not pre_process:
             return datasets
         datasets = self._post_process_datasets(datasets)
-        self._show_dataset(*datasets)
+        # Calling next(iter(...)) for a streaming dataset only on global rank 0
+        # advances that rank's persistent encoding/packing workers before the
+        # real dataloader starts. Other TP-coordinate DP groups do not perform
+        # the preview, so any stochastic preprocessing would diverge. Keep all
+        # groups at the same initial stream state; non-streaming datasets remain
+        # safe to preview.
+        if not args.streaming:
+            self._show_dataset(*datasets)
+        else:
+            logger.info('Skipping dataset preview for deterministic streaming state.')
         return datasets
 
+    @staticmethod
+    def _shard_streaming_dataset_by_dp(dataset, name):
+        if dataset is None:
+            return None
+        from megatron.core import mpu
+        dp_rank = mpu.get_data_parallel_rank(with_context_parallel=True)
+        dp_size = mpu.get_data_parallel_world_size(with_context_parallel=True)
+
+        def keep_rank(_row, index):
+            return index % dp_size == dp_rank
+
+        logger.info(f'Locally sharding {name} streaming dataset: DP rank {dp_rank}/{dp_size}')
+        return dataset.filter(keep_rank, with_indices=True)
+
     def _post_process_datasets(self, datasets: List) -> List:
         args = self.args
         predict_with_generate = getattr(args, 'predict_with_generate', False)
@@ -143,6 +169,7 @@ class SwiftSft(SwiftPipeline, TunerMixin):
                     num_proc=args.dataset_num_proc,
                     packing_length=args.packing_length,
                     packing_num_proc=args.packing_num_proc,
+                    packing_interval=args.packing_interval,
                     packing_strategy=args.packing_strategy,
                     strict=args.strict,
                     load_from_cache_file=args.load_from_cache_file)
